import { PagePlaceholder } from "@/components/page-placeholder";

export default function InventoryPage() {
  return (
    <PagePlaceholder
      title="Inventory"
      description="Stock levels, restock, adjustments and the movement ledger."
      phase="Phase 2"
    />
  );
}
